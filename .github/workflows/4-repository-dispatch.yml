name: "4-Repository-dispatch"

on: 
  repository_dispatch:
    type: [testando_repository_dispatch]
jobs: 
  build: 
    runs-on: "ubuntu-latest"
    steps:
      - run: echo "testando repository_dispatch"
        shell: bash